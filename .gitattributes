# Auto detect text files and perform LF normalization
* text=auto

# Force LF for specific files
*.tsx text eol=lf
*.ts text eol=lf
*.js text eol=lf
*.jsx text eol=lf
*.json text eol=lf
*.md text eol=lf

# Prevent backup files from being tracked
backup-* -text -diff
*_deleted_* -text -diff
*.backup -text -diff
old-* -text -diff
AUTH_* -text -diff
CLEANUP_* -text -diff
